<section class="all-bikes">

  <% for (let bike of bikes) { %>
    <div class="ui-card">
      <img src="<%= bike.image_url %>" alt="<%= bike.title %>">
      <section class="content">
        <h2>
          <a href="/bikes/<%= bike.id %>">
            <%= bike.title %>
          </a>
        </h2>
        <h4>at maccas</h4>
      </section>
      <footer class="content">
        <span>left</span>
        <span>right</span>
      </footer>
    </div>


<% } %>

</section>